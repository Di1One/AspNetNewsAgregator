@model IEnumerable<AspNetNewsAgregator.Core.DataTransferObjects.ArticleDto>

@{
    ViewData["Title"] = "View";
    //Layout = null;
}

<h1 id="article-header">Articles</h1>


<a href="~/article/create">Create article</a>

<div class="row">
    @if (Model.Any())
    {
        foreach (var item in Model)
        {
            @*@await Html.PartialAsync("ArticlePreview", item)*@
            await Html.RenderPartialAsync("ArticlePreview", item);
        }
    }
</div>

<div class="row">
    <button class="btn btn-primary col-12 col-sm-12 col-md-6 col-lg-4" onclick="changeBackgroundColorForPreviews()">
        Change Background colors
    </button>
</div>
@*@Html.GeneratePagintator(new int[]{1,2,3,4,5,6})*@


@section CssStyles
{
    {
    <style>
        h1 {
            color: red;
        }
    </style>
    }
}

@section Scripts
    {
    <script src="~/js/dom-samples.js" asp-append-version="true"></script>
}

    @* Don't use logic in views
@{
    var x = 1;
    var y = new List<int>()
    {
        1, 2, 3, 4, 5, 6
    };
    foreach (var i in y)
    {
        x += i;
    }
}
<div>@x</div>
*@
